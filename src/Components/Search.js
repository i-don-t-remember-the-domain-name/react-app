import React, { Component } from 'react';

export default class Search extends Component {
  render() {
    return (
      <form>
        <input placeholder="search hacker" />
        <button>Submit</button>
      </form>
    );
  }
}
